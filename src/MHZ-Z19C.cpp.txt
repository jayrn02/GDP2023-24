#include <MHZ.h>

#define MHZ19C_RX_PIN 10
#define MHZ19C_TX_PIN 11

MHZ mhz(MHZ19C_RX_PIN, MHZ19C_TX_PIN);



int readMHZSensor() {
    if (Serial.available()) {
        float data = Serial.read();
        // Rest of the code...
        Serial.print("Received data: ");
        Serial.println(data);
        return data; // Return the raw value
    }
    return 0; // Return 0 if no data available
}
